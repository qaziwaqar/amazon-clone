/**
 * Values safe to import into client components. `@/lib/auth` pulls in node:crypto and
 * the account store, so it must never cross the server boundary.
 */
export const DEMO_EMAIL = "demo@amazon-clone.dev";
export const DEMO_PASSWORD = "demo1234";
